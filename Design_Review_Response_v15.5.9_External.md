# listfiles Design.md 外部评审答复

> 评审来源：外部深度评审（匿名）  
> 答复者：Elune  
> 时间：2026-08-10  
> 对应文档版本：Design.md v15.5.9（commit 9d5884d）  
> 前置评审：Design_Review_Response_v15.5.9.md（Jimmy 评审答复，2026-08-09）

---

## 一、总评

这份评审的技术深度和系统性超过了上一版（Jimmy 个人评审）。它不仅是"找问题"，而是建立了一套完整的**正确性判定框架**——从目录任务生命周期到崩溃恢复矩阵，从输出提交语义到一致性模型声明。评审者显然有分布式系统或存储引擎的扎实背景。

核心判断：**评审结论正确，当前设计确实"防漏扫能力不达标"**。大部分 P0 问题要么在 Jimmy 的评审中被触及但未深化，要么是全新发现的隐蔽窗口。

以下逐项分类回应：**已修复 / 已规划 / 新发现 / 需澄清**。

---

## 二、P0 漏扫风险 —— 逐项分类

### P0-1：BATCH 与 FINISH 分通道，目录可能被误判已完成

**评审结论**：Worker 先发 BATCH 再发 FINISH，不代表 Master 先处理 BATCH。若 Master 先收到 FINISH，目录被标记完成，后续 BATCH 丢失。

**状态**：**新发现，且比上一版评审触及的更深。**

上一版评审（Jimmy）指出的"提交顺序不变量"集中在 **dpbin vs 输出落盘** 的持久化窗口。这一版评审发现了另一个独立窗口：**FINISH vs BATCH 的 IPC 时序**。

两个窗口的关系：
- Jimmy 版：dpbin 写了但输出没落盘 → 续传跳过 → 条目丢失
- 这一版：FINISH 处理了但 BATCH 还没到 → 目录标记完成 → 子目录永久不入队

**严重性**：后者更致命。前者丢的是文件条目（可审计），后者丢的是整棵子树（不可审计，因为 dpbin 认为已完成）。

**答复**：**承认。必须引入"任务完成屏障"。**

设计修正方向：
1. 目录任务从"派发"到"完成"引入显式状态机（见评审建议的 §6.1 目录任务生命周期）。
2. `RET_FINISH` 不再直接触发 `pending_tasks--` 和 dpbin 写入。必须等待该任务对应的所有 `pending_batches` 处理完毕、去重完成、子目录入队/持久化完成、输出偏移确认后，才允许 dpbin_append。
3. 终止条件必须加入：`pending_batches == 0`、去重线程池队列为空、IPC 返回队列为空、当前任务的所有 BATCH 已处理。

**与上一版 todo 的关系**：上一版 todo 中的"输出偏移屏障"（P0）只能解决 Jimmy 指出的窗口，不能解决这一版指出的 FINISH/BATCH 竞态。需要**新增一个独立设计项**。

---

### P0-2：pbin 先于输出提交，崩溃后文件可能永久丢失

**评审结论**：pbin 记录"已发现"，不代表"已输出"。续传从 pbin 恢复时认为已发现 = 已完成，不重新输出。

**状态**：**与上一版评审（Jimmy）的"提交顺序不变量"完全重合。**

上一版答复已提出"偏移量屏障"方案：dpbin 每条记录附加 `(path, output_offset)`，恢复时截断输出文件到最后一个已确认偏移。

**补充**：评审建议的 `DISCOVERED / OUTPUT_QUEUED / OUTPUT_COMMITTED` 三态比单纯的偏移量屏障更系统化。推荐采用。

**答复**：**已在 todo 中（P0），但方案需升级。**

从"偏移量屏障"升级到"三态状态机"：
- `DISCOVERED`：已写入 pbin，已去重，已分配目录任务或已进输出队列
- `OUTPUT_QUEUED`：已进 async_worker 队列
- `OUTPUT_COMMITTED`：async_worker 确认刷盘，返回已确认偏移

目录只有在该目录产生的所有文件条目都达到 `OUTPUT_COMMITTED` 后，才允许 dpbin_append。

---

### P0-3：fpbin 没有定义"二次崩溃"恢复路径

**评审结论**：第一次续传期间发现新目录 A 写入 fpbin，旧 pbin 未消费完时再次崩溃，第二次续传不读 fpbin，A 永久漏扫。

**状态**：**新发现。上一版评审只触及了 dspill 的恢复缺失，未触及 fpbin。**

**答复**：**承认。fpbin 的崩溃语义必须明确。**

设计修正：
1. `fpbin` 不再是"临时缓存"，而是"持久化待扫描队列"的一级。
2. 恢复时的读取优先级：
   ```
   待扫描目录 = (pbin - dpbin) ∪ fpbin ∪ dspill ∪ spbin(可恢复项)
   ```
3. "转正"操作必须原子化：旧 pbin 消费完毕 → fpbin 重命名为 `pbin.{next_seq}` → 更新 pump cursor。重命名过程中崩溃，下次恢复时同时检查 `fpbin` 和散落 `pbin.*`，不丢数据。
4. fpbin 格式必须与 pbin 一致（文本 + Footer），确保恢复逻辑统一。

---

### P0-4：背压兜底存在两套机制，职责冲突

**评审结论**：`pbin cursor` 和 `dspill` 同时作为背压回填来源，没有权威队列定义。更严重的是：目录写 pbin 后进 visited_set，但因背压没入队，只进 dspill，回填时 visited_set 误判为已访问而丢弃。

**状态**：**新发现，且比上一版评审指出的 dspill 恢复缺失更深层。**

上一版评审只说了"恢复时必须加载 dspill"，没发现**运行时的 visited_set 竞态**。

**答复**：**承认。这是运行时的真实漏扫路径。**

设计修正：
1. **统一权威待扫描队列**：`dspill` 作为唯一背压回填来源，`pbin cursor` 不再用于运行时回填（仅用于恢复时重建已发现集合）。
2. **visited_set 语义拆分**：
   - `discovered_set`：已发现（已写 pbin），防重复发现
   - `enqueued_set`：已入队（dispatch_queue 或 dspill），防重复入队
   - `completed_set`：已完成（dpbin），防重复扫描
3. 目录因背压未入队时，只进 `discovered_set`，不进 `enqueued_set`。从 dspill 回填时，查 `enqueued_set` 而非 `discovered_set`。

---

### P0-5：spbin 的恢复流程缺失

**评审结论**：续传流程只描述 `pbin - dpbin`，不读 spbin。被熔断目录永远不会重扫。

**状态**：**新发现。**

**答复**：**承认。**

设计修正：
1. spbin 必须纳入恢复路径：
   ```
   待扫描目录 = (pbin - dpbin) ∪ fpbin ∪ dspill ∪ spbin_recoverable
   ```
2. spbin 中记录跳过原因码（上一版 todo 已规划）。恢复时：
   - `SP_REASON_PROBE_FAIL` / `SP_REASON_TIMEOUT` → 重新入队（设备可能已恢复）
   - `SP_REASON_CIRCUIT_BREAKER` / `SP_REASON_PERMISSION` → 保持跳过（不可恢复）
3. 设备恢复后，probe_scheduler 负责将对应设备的 spbin 条目重新转为待扫描任务。

---

### P0-6：MSG_DROP 的存在意味着协议层允许丢结果

**评审结论**：MSG_DROP 只"销账"不补救，若 BATCH 被 drop 则结果丢失。

**状态**：**新发现。**

**答复**：**承认。MSG_DROP 的语义在当前设计中是危险的。**

设计修正：
1. **正常路径禁止 drop**：队列满时应背压，不是丢弃。
2. **若确实发生 drop（如内存耗尽）**：
   - 如果是 BATCH drop：受影响的目录任务整体标记为失败，重新入队或进 spbin
   - 如果是 FINISH drop：同上
   - 如果是 ERROR drop：必须终止运行（错误信息不可丢）
3. **任何 drop 发生后，本次运行不能标记为"完整完成"**，退出码必须为 1 或 2。

---

### P0-7：RET_ERROR 状态机没有定义当前目录的去向

**评审结论**：`BUSY -- RET_ERROR --> IDLE`，但 pending_tasks 是否减少、目录是否重入队、是否写 spbin 均未定义。

**状态**：**新发现。**

**答复**：**承认。状态机缺漏。**

设计修正：
1. `RET_ERROR` 触发后：
   - `pending_tasks--`（Worker 已释放）
   - 当前目录**不重入队**（避免重试风暴）
   - 写入 `spbin`，带原因码 `SP_REASON_PROBE_FAIL`
   - 设备进入 PROBING 态
2. 若探测后设备恢复：从 spbin 中捡回该目录，重新入队
3. 若探测后设备判死：目录保持跳过，退出码非零

---

### P0-8：没有"完整基准"标记

**评审结论**：上次扫描可能不完整（spbin 非空、dspill 未排空等），但没有机制阻止它成为盲信基准。

**状态**：**新发现。**

**答复**：**承认。必须引入 run manifest。**

设计修正（与评审建议 §6.3 一致）：
1. 每次运行生成 `{base}.manifest`：
   ```json
   {
     "run_id": "uuid",
     "target_path": "/public2/data",
     "root_device": "(fsid, server, export)",
     "schema_version": 1,
     "start_time": "...",
     "end_time": "...",
     "status": "complete|partial|failed",
     "stats": { "dirs": 123, "files": 456, "skipped": 0 },
     "cursors": { "pbin": "...", "dpbin": "...", "dspill": "..." },
     "checksum": "sha256",
     "baseline_eligible": true
   }
   ```
2. `baseline_eligible = true` 的严格条件：
   - `status == "complete"`
   - `skipped == 0`
   - `dspill` 已排空
   - `fpbin` 已转正
   - archive 完整性校验通过
3. 盲信扫描启动时，若 `baseline_eligible != true`，拒绝运行。
4. `finalize_archive()` 必须实现"保留旧基准、原子切换新基准"：新 archive 写完并校验通过后，才替换旧 archive。旧基准保留为 `.archive.prev`。

---

## 三、盲信扫描设计矛盾 —— 逐项分类

### 1. 不执行 lstat 就无法计算 dev+ino 指纹

**评审结论**：盲信声称"不 lstat"，但 fingerprint 需要 dev+ino，而 dev 必须通过 lstat/stat 获得。

**状态**：**需澄清——设计文档表述确实有矛盾，但实现层面有解。**

**答复**：**评审指出的逻辑矛盾成立，但可以通过以下方式消解：**

`dev` 的获取不必须对每个条目单独 `lstat`：
1. **目录级 dev 复用**：Worker 扫描目录 D 时，先对 D 本身做一次 `lstat` 获取 `st_dev`。D 下所有直接子项的 `dev` 与 D 的 `dev` 相同（除非跨越挂载点，但挂载点策略见 §5 其他缺口）。
2. **`readdir` 的 `d_ino` 字段**：在多数文件系统上（ext4/xfs/btrfs，以及 NFS 的 `readdir` 实现），`struct dirent` 的 `d_ino` 字段可以直接使用，无需额外 `lstat`。
3. **例外**：NFS 在某些配置下 `d_ino` 不可靠（如 `noshowmount`、`subtree_check`）。这种情况下盲信扫描不能依赖 `d_ino`，必须回退到 `lstat`。

**设计修正**：
1. 文档明确指纹计算的分层策略：
   - 优先：`path + d_ino`（`readdir` 直接获得，零额外 stat）
   - 次优：`path + dev + ino`（对目录做一次 `lstat` 获取 dev，子项复用）
   - 回退：全量 `lstat`（`d_ino` 不可靠时）
2. 程序启动时探测 `d_ino` 可靠性：扫描一小批样本，比对 `d_ino` 与 `lstat().st_ino`，不一致则回退。
3. 文档不声称"完全不做 lstat"，而是"**对盲信命中的条目不做 lstat**，对未命中/变更的条目正常 lstat"。

---

### 2. reference_map 对 Worker 进程未必可见

**评审结论**：Worker 是独立进程，若 reference_map 在 Worker 启动后加载到 Master 地址空间，Worker 看不到。

**状态**：**需澄清。**

**答复**：**评审的担忧在旧版实现中成立，但 v15.5.x 的设计已规避。**

当前 Design.md §5.3 明确：
> "Worker 扫描该目录（与 4.1 相同，但增加了盲信判断）"
> "计算 fingerprint(path, dev, ino) → 查 reference_map"

这个描述是**架构层面的伪代码**，不代表 Worker 进程直接访问 Master 的 `reference_map`。

实际实现（v15.5.x）的盲信判断在 **Master 侧的去重线程池中完成**：
1. Worker 发送 BATCH 到 Master（含所有条目路径 + `d_ino` 或 `lstat` 结果）
2. Master 的 batch_processor 计算 fingerprint，查 `reference_map`
3. 若命中且 mtime 未变，直接复用基准数据，不重新 lstat
4. 若未命中或 mtime 已变，标记为"需全量 lstat"，由后续流程处理

**设计修正**：文档 §4.3 需明确标注：盲信判断发生在 **Master 侧的 batch_processor**，不是 Worker 侧。Worker 仍发送完整条目信息，Master 决定是否复用基准。

---

### 3. skip_interval 的"保鲜期"语义错误

**评审结论**：`skip_interval` 检查的是"pbin 记录的 mtime 距今多久"，不是"文件自基准后是否变更"。旧 mtime 的文件被修改后仍会被盲信跳过。

**状态**：**已部分修复，但设计文档表述仍不准确。**

当前 Design.md §4.3 已修正为：
> "盲信跳过的目录不会出现在输出中（因为不扫描）"
> "盲信扫描的输出是'上次全量结果 + 本轮新增条目'的混合"
> "盲信扫描期间文件系统应无变更"

但 `--skip-interval` 的语义描述仍可能误导：
> "`skip_interval` 参数的含义是'保鲜期阈值'：pbin 中记录的 mtime 距今未超过此秒数（文件近期被修改过）则不盲信，需重扫；超过此秒数（文件很久未变）则允许盲信"

**答复**：**承认语义描述仍需修正。**

`skip_interval` 的真实语义不是"保鲜期"，而是"**基准年龄阈值**"：
- 基准（pbin 记录）的 mtime 距离今天的时间 → 表示"这个基准有多旧"
- 它**不能**检测"文件自基准建立后是否变更"
- 用途：排除"基准太旧、不可信"的条目，强制重扫

**设计修正**：
1. 将 `--skip-interval` 改名为 `--baseline-age-limit` 或 `--max-baseline-age`（破坏性变更，需评估）。
2. 文档明确声明：该参数**不检测变更**，只控制"是否信任旧基准"。
3. 若要检测变更，需要文件系统变更号、快照代际等机制，当前设计不支持。

---

### 4. pbin 记录的信息不足以支持"复用完整 stat 数据"

**评审结论**：pbin 只记录路径、dev、ino、mtime、d_type，若输出需要完整 stat 元数据则不足。

**状态**：**需澄清。**

**答复**：**当前 pbin 的字段足够支撑当前输出 schema。**

当前输出格式（`--csv`）的字段：path、size、mtime、mode、uid、gid、nlink、ino、dev、d_type。pbin 记录的字段：path、dev、ino、mtime、d_type。**缺少 size、mode、uid、gid、nlink**。

评审正确。盲信模式下这些字段会从 pbin 复用，但 pbin 不存它们 → 输出缺失。

**设计修正**：
1. pbin schema 扩展：增加 `st_size(8)、st_mode(4)、st_uid(4)、st_gid(4)、st_nlink(4)`。
2. 或者：盲信模式下输出 schema 降级，只输出 path、dev、ino、mtime、d_type，其余字段留空或标记为 `<baseline>`。
3. 推荐方案 1（扩展 pbin），因为存储成本可忽略（每行增加 24 字节，12 亿文件 ≈ 28GB，gzip 后约 3-5GB）。

---

### 5. 盲信扫描本质上不是"扫描"，而是"基准回放 + 新增发现"

**评审结论**：盲信不能发现已有条目的变更，不能检测删除，没有 tombstone。不能叫"增量扫描"或"变更扫描"。

**状态**：**已部分修正。**

当前 Design.md §4.3 已声明：
> "盲信扫描的输出是近似快照，不是精确当前状态"
> "用于快速复现上次结果或处理新增条目，不用于变更检测"

**答复**：**同意评审的命名建议。**

文档 §2.1 设计目标中的"半增量扫描"应改为：
> "**基准重放扫描（Baseline Replay Scan）**：在已有完整基准的前提下，复用上次记录的元数据，仅对新增条目执行全量扫描。输出为'基准回放 + 新增发现'的混合，不保证变更检测、不保证删除可见。"

---

### 6. 盲信扫描对"目录枚举失败"的输出语义未定义

**评审结论**：若目录 D 因设备错误无法 readdir，旧基准结转会输出假存在，不结转则大规模漏输出。

**状态**：**新发现。**

**答复**：**承认。**

设计修正：
1. 目录枚举失败时，该目录及其整棵子树标记为 `UNKNOWN / INCOMPLETE`。
2. 该目录**不计入 completed_set**，**不进 dpbin**。
3. 该目录的旧基准**不结转**到本轮输出（避免假存在）。
4. 该目录进 spbin，带原因码 `SP_REASON_PROBE_FAIL` 或 `SP_REASON_TIMEOUT`。
5. 本次运行不能被标记为"完整完成"。

---

## 四、NFS 容错假设修正

### `soft,intr,timeo=600,retrans=3` 的问题

**评审结论**：
1. `timeo=600` 单位是十分之一秒（60秒），不是 600 秒。
2. `intr` 在 2.6.25+ 被忽略。
3. TCP NFS 重传退避后总耗时可能超过 120s 心跳超时。
4. `soft` 挂载超时后返回错误，带来数据一致性风险。

**状态**：**需澄清——评审的技术细节正确，但结论需区分。**

**答复**：**承认技术事实，但需区分设计意图与实际约束。**

1. **`timeo=600` 确实是 60 秒**：文档 §12 的挂载参数写错了。应为 `timeo=6000` 才是 600 秒。修正。
2. **`intr` 确实在 CentOS 7.4 上无效**：文档不应将 `intr` 作为关键前提。修正为 `soft,timeo=6000,retrans=3`。
3. **心跳超时 120s 可能误判慢请求**：v15.5.9 的 `scanner_progress_tick`（5s 时间驱动）已部分缓解，但评审指出的"TCP 退避 > 120s"场景确实存在。
   - 修正：心跳判活条件从"心跳到达"升级为"心跳到达且计数前进"。
   - 补充：读 `/proc/<pid>/wchan` 区分 D-State 与背压阻塞。
4. **`soft` 挂载的风险**：评审正确，`soft` 在超时后可能返回不完整数据。但这是"元数据操作"（readdir/lstat），不是数据读写，风险有限。`hard` 挂载下 D-State 不可杀，风险更大。
   - 文档需声明：本工具假设元数据操作在 soft 挂载下的超时错误是可检测的（返回错误码），不假设数据一致性由挂载参数保证。

---

## 五、其他缺口 —— 与上一版评审的交叉对照

| 缺口 | 评审 | Jimmy 评审 | 状态 |
|------|------|-----------|------|
| 活跃文件系统一致性 | 未定义 | 未触及 | 新发现，P1 |
| MAX_PATH_LENGTH = 4088 | 绑定 IPC 原子写，应分离 | 未触及 | 新发现，P1 |
| pbin 文本格式对任意文件名不安全 | 需长度前缀或转义 | 未触及 | 新发现，P0 |
| 条目级错误分类 | 需完整矩阵 | 已部分覆盖（errno 分类） | 深化，P1 |
| 挂载点/symlink/bind mount | 策略缺失 | 未触及 | 新发现，P1 |
| strict_nlink 局限性 | 仅辅助断言 | 未触及 | 新发现，P2 |
| 多实例并发防护 | 需 flock + 参数校验 | 已部分覆盖（双跑防护） | 一致，P2 |
| 输出文件续写语义 | 缺失 | 未触及 | 新发现，P1 |
| 计数器宽度 | 需 64 位 | 未触及 | 新发现，P2 |
| 内存可行性 | 缺少证明 | 已触及（fingerprint_set 容量） | 深化，P1 |

---

## 六、文档矛盾 —— 与上一版评审的交叉对照

| 矛盾 | 评审 | Jimmy 评审 | 状态 |
|------|------|-----------|------|
| IPC 消息方向表错误 | §7.2 混了 M→W 和 W→M | 未触及 | 新发现，需修正 |
| pbin 写入者前后不一致 | §4.1 vs §5.1 | 未触及 | 新发现，需澄清 |
| "pbin 记录所有扫描过的条目" | 应为"已发现"而非"已扫描" | 未触及 | 新发现，需修正 |
| Monitor 与 Main 进程收割重复 | §5.1 vs §5.4 | 未触及 | 新发现，需明确 |
| "新文件/变更文件输出" | 与盲信语义矛盾 | 未触及 | 新发现，需修正 |
| "减少 90%+ I/O" | 无论证 | 未触及 | 新发现，需删除或补论证 |

---

## 七、缺失章节 —— 优先级评估

评审建议补充的章节：

1. **目录任务生命周期**（§6.1）—— **P0，必须在下一版设计文档中加入**
2. **崩溃恢复矩阵**（§6.2）—— **P0，必须补充**
3. **持久化 manifest**（§6.3）—— **P0，必须补充**
4. **输出完整性语义**（§6.4）—— **P1，需补充**
5. **容量模型**（§6.5）—— **P1，需补充**

---

## 八、修订后的待办清单（合并两版评审）

### P0（阻塞发布）

| 编号 | 事项 | 来源 | 状态 |
|------|------|------|------|
| P0-001 | 任务完成屏障：FINISH 不直接触发完成，需等 BATCH 全处理 + 子目录入队 + 输出偏移确认 | 本版 P0-1 | 新 |
| P0-002 | 输出三态状态机：DISCOVERED → OUTPUT_QUEUED → OUTPUT_COMMITTED | 本版 P0-2 / Jimmy 版 | 升级 |
| P0-003 | fpbin 二次崩溃恢复：恢复时读取 fpbin，转正原子化 | 本版 P0-3 | 新 |
| P0-004 | visited_set 语义拆分：discovered / enqueued / completed 三态 | 本版 P0-4 | 新 |
| P0-005 | spbin 纳入恢复路径：可恢复项重新入队 | 本版 P0-5 | 新 |
| P0-006 | MSG_DROP 正常路径禁止，drop 后整任务重扫或运行失败 | 本版 P0-6 | 新 |
| P0-007 | RET_ERROR 状态机补全：pending_tasks--、写 spbin、不重入队 | 本版 P0-7 | 新 |
| P0-008 | Run manifest + baseline_eligible 原子切换 | 本版 P0-8 | 新 |
| P0-009 | 目录任务生命周期状态机文档 | 本版 §6.1 | 新 |
| P0-010 | 崩溃恢复矩阵（12+ 个崩溃点） | 本版 §6.2 | 新 |
| P0-011 | 半增量跳过前提条件声明（已有一版修正，需继续） | Jimmy 版 / 本版 | 已部分 |
| P0-012 | epoch + waitpid 确认（旧 Worker 残留数据） | Jimmy 版 | 已规划 |

### P1（重要，不阻塞但需尽快）

| 编号 | 事项 | 来源 |
|------|------|------|
| P1-001 | 恢复时加载 dspill 并集 | Jimmy 版 |
| P1-002 | errno 分类矩阵 | Jimmy 版 / 本版 |
| P1-003 | 设备身份主键改为 (fsid, server, export) | Jimmy 版 |
| P1-004 | 毒丸目录清单（致死 3 次隔离） | Jimmy 版 |
| P1-005 | 设备级熔断 DEGRADED 灰度态 | Jimmy 版 |
| P1-006 | IPC 协议显式字段编码 + protocol_version | Jimmy 版 |
| P1-007 | 输出完整性语义声明 | 本版 §6.4 |
| P1-008 | 输出文件续写语义（追加 vs 覆盖、去重对齐） | 本版 |
| P1-009 | pbin 文本格式 → 二进制安全编码（长度前缀） | 本版 |
| P1-010 | 盲信目录枚举失败的输出语义 | 本版 |

### P2（补充完善）

| 编号 | 事项 | 来源 |
|------|------|------|
| P2-001 | 一致性模型声明（快照隔离近似） | Jimmy 版 / 本版 |
| P2-002 | 孤儿 Worker 自裁（PR_SET_PDEATHSIG） | Jimmy 版 |
| P2-003 | 续传配置校验（版本、格式、标志） | Jimmy 版 |
| P2-004 | 进度文件位置声明（建议本地盘） | Jimmy 版 |
| P2-005 | 容量模型（内存、磁盘、吞吐） | 本版 §6.5 |
| P2-006 | 长路径、非 UTF-8 文件名、换行文件名处理 | 本版 |
| P2-007 | 挂载点、符号链接、bind mount 策略 | 本版 |
| P2-008 | 计数器、游标、偏移全部 64 位 | 本版 |
| P2-009 | 全链路资源有界性论证 | Jimmy 版 |
| P2-010 | 自适应并发控制（AIMD） | Jimmy 版 |
| P2-011 | 输出按大小分片 | Jimmy 版 / 本版 |

---

## 九、结论

这份评审的核心价值在于建立了一套**从"高性能容错扫描器"到"不漏扫的生产级设计"的完整检查清单**。它指出的 8 个 P0 问题中，有 7 个是上一版评审未触及的全新漏扫窗口，尤其是：

1. **FINISH/BATCH 竞态**（P0-1）：比持久化窗口更隐蔽
2. **fpbin 二次崩溃**（P0-3）：恢复逻辑的死角
3. **visited_set 背压竞态**（P0-4）：运行时的真实路径
4. **spbin 恢复缺失**（P0-5）：熔断目录的永久丢失
5. **MSG_DROP 语义**（P0-6）：协议层的静默丢数据
6. **RET_ERROR 状态机缺漏**（P0-7）：目录去向未定义
7. **baseline 完整性标记**（P0-8）：不完整基准被误用

**修订后的正确性目标**（采纳评审建议）：

> **全量扫描**："至少发现一次、输出可恢复、重复可去重、失败可审计"。  
> **盲信扫描**：明确降级为"**近似基准回放 + 新增发现**"，不是变更检测。  
> **终止语义**：退出码区分 0（完全完成）、1（部分完成）、2（严重失败）。  
> **一致性模型**：快照隔离近似，不保证全局一致快照，每个目录的枚举+stat 原子，跨目录不保证。

当前待办清单从上一版的 16 项扩展为 **33 项**，其中 P0 项从 3 项增加到 **12 项**。在进入代码实现前，这些 P0 项的设计必须全部闭环。
